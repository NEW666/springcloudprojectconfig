#### 配置网关反向代理    
zuul:
  routes:
    api-member:
     ### 以 /api-/访问转发到会员服务
      path: /api-/**
      serviceId: app-member
    api-order:
        ### 以 /api-/访问转发到订单服务
      path: /api-/**
      serviceId: app-order

ribbon: 
  eureka: 
    enabled: true
